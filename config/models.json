{
  "models": {
    "glm-4-flash": {
      "provider": "zhipuai",
      "role": "primary",
      "description": "Fast, capable general model for everyday Ellen responses",
      "api_key_env": "GLM_API_KEY",
      "max_tokens": 2048,
      "temperature": 0.8
    },
    "claude-haiku-4-5": {
      "provider": "anthropic",
      "role": "fallback",
      "description": "Used for sensitive topic routing and when primary model is unavailable",
      "api_key_env": "ANTHROPIC_API_KEY",
      "max_tokens": 2048,
      "temperature": 0.7
    }
  },
  "routing_rules": [
    {
      "name": "sensitive_topic_filter",
      "description": "Route politically sensitive topics through Claude Haiku for safer deflection",
      "trigger": "topic_match",
      "topics": "openclaw.json#model_routing.sensitive_topics",
      "target_model": "claude-haiku-4-5"
    },
    {
      "name": "fallback_on_error",
      "description": "If GLM-4-Flash errors or times out, fall back to Claude Haiku",
      "trigger": "model_error",
      "source_model": "glm-4-flash",
      "target_model": "claude-haiku-4-5"
    }
  ]
}
