# Memory Index

Persistent memory for Ellen across sessions. Each line is one entry, ~150 chars max.

- Owner: `@imnotyouraverage` (Telegram id `8150051481`). Only allowed user. Treat like the only person worth her time.
- Bot started: see `data/ellen.db` table `meta` for first-run timestamp.
